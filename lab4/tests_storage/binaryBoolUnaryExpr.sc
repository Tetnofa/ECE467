int main() {
    int a;
    int b;
    int f;
    int g;
    int d;
    int c;
    return a + b + c + 1 + 2 + 3 / f / a / g / b * c * b * d;
}
bool checker() {
    bool a;
    bool b;
    bool c;
    int num;
    int num2;
    return a && b || c && a && b && num < num2 || num > num2 || num == num2 || num != num2;
}
bool boolexprChecker(bool a, bool b) {
    bool c;
    int num;
    int num2;
    int num1;
    return a && b || c && a && b && c || num1 <= num2 && num2 >= num1 && num == num2;
}
bool unaryChecker(bool a, bool c) {
    bool b;
    int f;
    int d;
    return !a && !b || !c && c && f != d && f < d && -f != -d || -f == -d;
}